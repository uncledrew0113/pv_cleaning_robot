# PV 清扫机器人 — 构建环境变量模板
#
# 使用前执行：
#   source .env.example
# 或按实际 SDK 安装位置修改后 source。
#
# 设置完成后运行：
#   cmake --preset rk3576-cross-linux
#   cmake --build --preset rk3576-build
export RK3576_SDK_PATH=/home/tronlong/RK3576
